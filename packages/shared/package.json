{
  "name": "@repo/shared",
  "version": "0.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'build not configured'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}
